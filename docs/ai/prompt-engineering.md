Documento donde alojaré todos los prompt utilizados y comentaré que es lo que mejor funciona en cada caso.

Prompts:

Para generar código: 

    Quiero hacer un pequeño script que pruebe cada función del proyecto NextUp antes de empezar a refactorizar el código para asegurarnos a cada paso que no se rompe nada.

    Dentro de @NextUp/index.html quiero aplicar programación responsiva al botón theme-toggle, quiero que para pantallas pequeñas tipo smartphone se sustituya su estética y en vez de los textos se vean los svg que tengo en mi carpeta imágenes, moon-fill para poner dark mode y sun-fill para quitarlo, no quiero cambiar nada de la funcionalidad del botón, solo su estética cuando la pantalla sea tamaño smartphone.

    En index.html recomiendame un color de fondo para las tareas generadas y para su texto, tanto en modo claro como oscuro.
    escribe las clases de tailwind donde corresponda en index.html

Para refactorizar:

    Viendo mi @NextUp/index.html y mi app.js que recomendaciones me das para refactorizar el código y haz una lista simple de las partes que necesitan refactorizarse.

    Empecemos a refactorizar @NextUp/index.html yendo punto por punto, siguiendo la lista previamente hecha.

    Se puede mejorar la estructura de los archivos dentro de NextUp, haz una restructuración pero @NextUp/index.html quiero que esté en la raiz del proyecto y la carpeta Imagenes tambien.

Para documentar el proyecto:

    Haz un archivo de texto.md a modo de "historial" de cambios para saber que estamos refactorizandio en detalle
    Añade comentarios JSDoc en varias funciones del proyecto.
    Comenta en español los cambios realizados para que esté, igual que el resto del código previo.
    Mediante MCP: “Genera documentación automática de app.js”
    Mediante mcp: “Analiza este repositorio y genera un resumen técnico del proyecto” 

Para que cada prompt funcione bien es necesario ser lo más claro posible a la hora de pedir a la ia que haga algún cambio en el código, es interesante acotar su funcionamiento a funciones concretas y especificar que debe centrarse en mantener la funcionaliodad original.

Si previamente le pedimos que comente el código de forma clara usando JSDoc luego podrá hacer mejor documentación.

